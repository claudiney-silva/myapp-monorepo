{{- if and (.Values.appBar.enabled) (.Values.appBar.scaledObject) }}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ .Values.appBar.label }}-scaledobject
spec:
  scaleTargetRef:
    name: app-bar
      {{- toYaml .Values.appBar.scaledObject | nindent 2 }}
{{- end }}