{{- if .Values.bar.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: boleto-provider-core
spec:
  rules:
  {{- range .Values.bar.ingress.hosts }}
  - host: {{ .name }}
    http:
      paths:
        - path: {{ default "/" .path }}
          pathType: {{ default "ImplementationSpecific" .pathType }}
          backend:
            service:
              name: {{ default "bar" .serviceName }}
              port:
                number: {{ default 80  .servicePort }}
  {{- end }}
{{- end }}